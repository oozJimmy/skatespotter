Bootstrap library for UI/UX
Google Maps Platform to implement spot mapping 

todo:
-improve website theme( add big picture section under header,inside header have nav buttons (map, add spot, spot list))
-at least a couple animations

ideas/extensions:
-add spot pictures / if no picture submitted googlemaps satellite picture used?
STOP

compile scss:
sass scss/custom.scss src/bootstrap.css